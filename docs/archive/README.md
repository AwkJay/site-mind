# Archive

Pre-build specs and deferred-scope docs, kept for provenance rather than deleted outright — each was the
plan an implementing session worked from, and the corresponding "done" record lives in `../../PROGRESS.md`'s
slice log. None of these describe the current live product; read `../ARCHITECTURE.md` and the root
`README.md` for that.

- `BUILD_PLAN_NEXT.md`, `BUILD_PLAN_NEXT2.md`, `BUILD_PLAN_NEXT3.md` — the three build-phase specs
  (ROI/cost-risk, multi-agent mitigation/alerting/hybrid retrieval, UI-review fixes). All shipped; see
  `PROGRESS.md` for what was actually built against each.
- `DEPLOY.md` — free-tier Vercel+Render deploy steps. Written but never run — delivery for this
  submission is recorded video + repo, not a live URL (a deliberate scope decision, not an oversight).
